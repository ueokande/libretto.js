#
# Scena.Navigator class
#

class window.Scena.Navigator
  constructor: (@dom) ->

  updatePage: (index) ->

  updateAll: ->

  movePage: (from, to) ->

  deletePage: (index) ->

  addPage: (before) ->

  currentPage: (before) ->

